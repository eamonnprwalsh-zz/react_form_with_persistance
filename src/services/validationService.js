export const isNumberBetween17and100 = (num) => {
  return num.length === 0 || (num > 17 && num < 100);
};

export const isDateXDaysInThePast = (date, limit) => {
  console.log('In isDateXDaysInThePast');
  return;
};
